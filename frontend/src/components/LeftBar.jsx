import { Canvas, Circle, Line, Rect, Textbox, Triangle } from 'fabric';
import React, { useEffect } from 'react'

const LeftBar = ({boxRef,canvasRef,projectId,selectedColor,selectedShape,setActiveObject,setActiveTool,setIsMoving,setSelectedShape,setShowDeleteButton,isDrawingMode,isMoving,isSame}) => {
  
  return (
   <>
   </>
    
  )
}

export default LeftBar
